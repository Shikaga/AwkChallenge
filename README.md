"npm install" //Start by running

"heroku create" //Create Heroku Project

"heroku login" //Login to heroku:

"heroku create" //Create the data to push to Heroku

"git push heroku master" //Push it to your heroku instance
